{
  "name": "auralprint-refactor",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/watch.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
